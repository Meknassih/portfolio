<!DOCTYPE HTML>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Hamza El Meknassi - {{ title or metadata.title }}</title>
    <meta property="og:title" content="Hamza El Meknassi - {{ title or renderData.title or metadata.title }}">
    <meta name="apple-mobile-web-app-title" content="Hamza El Meknassi - {{ title or renderData.title or metadata.title }}">
    <meta name="twitter:title" content="Hamza El Meknassi - {{ title or renderData.title  or metadata.title }}">
    <link rel="alternate" href="{{ metadata.feed.path | url }}" type="application/atom+xml" title="{{ metadata.title }}">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="apple-mobile-web-app-status-bar-style" content="white">
    <meta name="format-detection" content="telephone=no">
    <!-- Icon -->
    <link rel="icon" type="image/png"  href="{{ favicon }}">
    <meta name="msapplication-TileColor" content="#1a1a1a">
    <meta name="theme-color" content="#f0f0f0">
    <!-- Description -->
    <meta name="description" content="{{ description }}">
    <meta property="og:description" content="{{ description }}">
    <meta name="twitter:description" content="{{ description }}">
    <!-- Twitter -->
    <meta name="twitter:card" content="summary">
    <!-- CSS -->
    {% set css %}
    {% include "css/main.css" %}
    {% endset %}
    <style>
      {{css | cssmin | safe}}
    </style>
  </head>
  <body>
    <h1 class="bcg-text">
      {% for i in range(0, 16) -%}
        web http nodejs jwt gzip typescript dns html netlify css postgresql javascript ssh reactnative api firebase 11ty angular ux nunjucks scss network vuejs tls bulma webpack json xml rest nestjs mule markdown git curl react socket materialui openapi i18n pwa svelte python bash ui mongodb markdown git tailwindcss{{" "}}
      {%- endfor %}
    </h1>
    <div class="container">
      <div class="box">
        <section>
          {% include "components/header.njk" %}
          <div class="content">
            {{ content | safe }}
          </div>
        </section>
      </div>
    </div>
    {% include "components/footer.njk" %}
  </body>
</html>